// function formatNYTQueryParams(params) {
//     const NYTqueryItems = Object.keys(params)
//         .map(key => `${encodeURIComponent(key)}=${encodeURIComponent(params[key])}`);
//     return NYTqueryItems.join('&');
// }
